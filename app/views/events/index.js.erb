$('.events-day-content').append('<%= escape_javascript(render partial: "events") %>');
$('#btn-load-more').attr('href', '<%= escape_javascript team_events_path(@team) + ".js?page=#{@next_page}" %>')

