.event.event-common.event-todo-due_at_changed
  %a.from{:href => "#{team_user_path(current_team, event.actor)}", :target => "_blank"}
    %img.avatar{:alt => "#{event.actor.nickname}", :src => "#{gravatar_image_url(event.actor.email)}"}/
  %i.icon-event
  .event-main
    .event-head
      %a.event-created-at
        = event.created_at.strftime("%H:%M")
      %span.event-actor
        %a.link-member{:href => "#{team_user_path(current_team, event.actor)}", :target => "_blank"}
          = event.actor.nickname
      %span.event-action
        将任务完成时间从
        %span{"data-readable-date" => ""} 
          - if event.extra[:old_date].nil?
            没有截止日期
          - else
            = event.extra[:old_date].strftime("%m月%d日")
        修改为
        %span
          = event.extra[:new_date].strftime("%m月%d日")
      %span.event-text
        %span.emphasize
          %a.todo-rest{"data-stack" => "true", :href => "#{project_todolist_path(event.project, event.eventable)}", :title => ""}
            = event.eventable.content